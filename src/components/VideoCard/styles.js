import { VStack } from 'native-base';
import styled from 'styled-components/native';

export const Container = styled(VStack)`
  width: 100%;
`;

export const CardPlay = styled.TouchableOpacity`
    
`;
